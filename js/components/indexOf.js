/*
  * 検索文字の場所
  * 入力の文字が見つかれば、何文字目か答えるアプリ
*/
const book = "吾輩は猫だよ"
r(book)

// break
r('<p></p>')

/* const pos = book.indexOf('猫')
r(pos)
 */

/* let keyword = prompt('検索文字')
let pos = book.indexOf(keyword)

if (pos > 0){
  r(`${keyword}は${pos + 1}文字目です`)
} else {
  r('ない！')
} */